# 24 小时重启系统 · 产品设计文档 v1

> 人生改变的起点不是突然变得更强，而是让大脑每天至少收到一次真实的胜利信号。

## 1. 一句话定位

一个**仪式引导器（ritual scaffolding）**：在一天的正确时间点，把提前设计好的正确动作递到用户面前，替他消掉"现在该干嘛"的决策。以**可见交付物**为中心的一天闭环，而不是以任务列表为中心。

它不是 todo list、不是笔记软件、不是番茄钟、不是习惯打卡——虽然每个各取 10%。

## 2. 已锁定的决策

| 决策项 | 选择 |
|---|---|
| 产品形态 | Web App / PWA（可安装到主屏，本地优先） |
| 范围 | 完整六模式一次做全（自用，无账号无后端） |
| 目标用户 | 先只给自己用，按真实节奏迭代 |
| 游戏化程度 | 中等：链条 + 成果墙（热力图 + 交付物列表），无成就徽章 |

## 3. 设计北极星

1. **App 的 KPI 是"今天有没有产生一个可见交付物"，不是使用时长。**一个好用的日子，一天在 App 里花的总时间应少于 3 分钟。反制"陷阱二：工具成瘾"。
2. **界面 = 时间轴状态机**，永远只回答一个问题："现在，下一步做什么？"打开只看到一张卡片，没有 dashboard。
3. **意志力外包给默认值。**模板预填、一键开始、通知即填空题。凡需要现场决策的地方都是设计失败。
4. **中断 ≠ 失败，是产品设计问题。**内置断链保护（失控日应急模式）。
5. **永不责备。**错过仪式 = 静默降级为压缩版，界面永远不出现红色失败态。App 是教练，不是法官。

## 4. 一天的状态机

### 4.1 时段与模式（时间窗可配置，此处为默认值）

| 默认时段 | 模式 | 一句话 |
|---|---|---|
| 20:30–23:00 | 🌙 晚间复合流程 | 复盘 15 分钟 + 前夜清障 30 分钟，一气呵成 |
| 07:00–08:00 | ☀️ 晨间启动 | 醒后 60 分钟，确认 MIT，进入主动产出模式 |
| 08:00–12:00 | 🎯 深度执行 | 90–120 分钟专注循环，每轮有交付承诺 |
| 12:00–14:00 | 🔄 午间复位 | 三行复位卡 + 二段推进开始 |
| 14:00–18:00 | ➡️ 二段推进 | 铁律：上午做什么下午继续做什么；结束写明日锚点 |
| 随时 | 🚑 应急模式 | 失控日三步：压缩成 10 分钟版 → 一个微交付 → 写明日锚点 |

### 4.2 转换规则

- 时间窗只决定**默认显示**哪张卡，不锁死；任何时候可手动切换模式。
- 每个仪式都有**错过兜底**：压缩版入口（如 30 秒 MIT、一行复盘）。
- 错过 = 静默降级，绝不显示惩罚性 UI。

### 4.3 接力机制（灵魂）

昨夜写的「明天 8:00–8:30 我要完成 ___」= 今晨打开 App 的第一张卡。
二段推进结束时先记一版粗锚点，晚间清障时完善确认——两者写向同一个 Anchor 对象。日与日之间靠锚点缝合成闭环。

## 5. 数据模型

```ts
Day {
  date: string              // YYYY-MM-DD
  mit: string               // 今日唯一 MIT（只能一个）
  deliverable: {            // 今日可见交付物
    text: string
    proofUrl?: string       // 可选：截图/链接/文件路径
    loggedAt: timestamp
  } | null
  status: 'won' | 'emergencyWon' | 'lost'   // lost = 无记录（派生）
  morningDone: boolean
  resetCard: { morningDid, afternoonOne, startAt } | null
  focusSessions: [{
    startedAt, minutes,
    commitment: string      // 本轮交付承诺
    result: 'done' | 'downgraded' | 'abandoned'
  }]
  review: { best, blocker, action, keep, drop } | null   // 复盘 5 问
  anchor: { nextStep, where, startTime, ifThen? } | null  // 写给明天，晨间接力预填
  eveningDone?: boolean                                   // 晚间复合流程完成标记
}

Rule { id, text, uses, updatedAt }   // If-Then 规则库（一句式，降低夜间填写摩擦）
Settings { timeWindows, soundOn, ... }
Chain                               // 不落库，由 Days 派生计算
```

- **本地优先**：IndexedDB 存储，数据可导出 JSON / Markdown。
- 无账号、无后端、无埋点。

## 6. 六大模式逐屏设计

### 6.1 🌙 晚间复合流程（20:30，约 45 分钟）

一屏一步，可跳过任何一步：

1. **复盘 5 问**（逐屏滑动）：今天最有效的动作？最大阻力？阻力出现时我做了什么？明天保留哪个？删除哪个？
2. **记录今日交付物** → 若还没有：引导进入应急模式（10 分钟版）。
3. **写明日第一项任务**：填空模板「明天 __:__–__:__，我要完成 ___」。
   - **交付物自检**：「这个成果能截图、能提交、能被别人看到吗？」过滤"思考一下/学习一下"这类动词型目标。
   - 快选模板：写完 200 字草稿 / 列出 10 个要点 / 3 个标题 + 3 个卖点 / 修复并提交一个代码变更。
4. **清障清单**（勾选）：桌面清空 / 文档打开停在光标处 / 资料同一文件夹 / 计时器就位 / 手机客厅充电 / 短视频已限制。
5. **写一条 If-Then**：从规则库选一条或新建（示例：如果明早想拖延，我就先启动计时器只做 5 分钟）。
6. **启动理由**一句话：「明天我只做一件事：让系统跑起来」→ 完成屏「明天见」。

### 6.2 ☀️ 晨间启动（醒后 60 分钟）

- 极简 4 步：起床（不按稍后提醒）/ 光 + 水 / 3–5 分钟低强度活动 / 确认 MIT（昨夜锚点已填好，只确认或微调）。
- 出口是一个大按钮：**「开始第一轮（25 / 45 分钟）」**。
- 明确不做：信息流、新闻、数据统计。这一屏的存在意义是让大脑进入主动模式。

### 6.3 🎯 深度执行（上午 90–120 分钟）

- **启动前置条件**：必须填写「这一轮结束我交付 ___」才能开始计时（快选模板同上）。
- 计时 25 / 45 分钟可选；结束用声音 + 通知提醒。
- **技术要点**：以时间戳计算剩余时间（禁止 setInterval 累加，防后台节流漂移）；Screen Wake Lock API 可选保持常亮；锁屏不影响计时。
- 中途两个按钮：
  - **「卡住了」** → 弹出降级动作建议（按任务类型：写不出方案→先列 5 个要点；写不出文案→先写想说的话）。选择后**计时器不重置、任务不切换**。
  - **「放弃这轮」** → 记录 abandoned，无任何惩罚文案。
- 每轮结束勾选「交付完成？」→ 写入 focusSessions。

### 6.4 🔄 午间复位 + 二段推进

- **三行复位卡**：上午完成了什么（自动带入）/ 下午最重要的一件事 / 几点开始（快捷时间选择）。
- 二段推进 60–90 分钟，界面显示铁律：「上午做什么，下午继续做什么」。
- 结束时记粗版**明日锚点**：下一步是什么 / 文件在哪里 / 明天几点开始（晚间流程里完善确认）。

### 6.5 🚑 应急模式（失控日）

- 入口：首页常驻小按钮「今天崩了」+ 22:00 仍未记录交付物时的温和推送。
- 三步：把任务压缩成 10 分钟版本（给降级模板）→ 完成一个微交付并记录 → 写明日锚点。
- 记为 `emergencyWon`（**灰色胜利**），链条不断。
- 文案基调：「一天被打乱很正常。真正毁掉持续性的不是中断，是中断后彻底放弃。」

### 6.6 🏆 成果墙与链条

- 月历热力图：绿 = won，灰 = emergencyWon，空 = lost。
- 点开某天：当天交付物、MIT、专注轮次、复盘摘要。
- 链条数 = 连续非空天数；断链文案：「重新开始也是系统的一部分」。
- **赢的门槛刻意压低**：记录了交付物（含应急版）即 won；复盘 5 问是加分项，不是及格线。

## 7. If-Then 规则库

- 前夜选一条或新建；深推「卡住了」和晨间卡片可展示用户自己写的规则原话。
- 记录使用次数 → 复盘第 4 问「明天保留哪个动作」有了数据依据。
- 自己写的规则由自己执行，比任何 App 说教有效。

## 8. 通知策略（PWA 现实约束）

- 每天最多 3 条：
  1. 20:30 晚间流程：「明天 __:__ 你要完成什么？现在写下，明早就不用想了。」
  2. 晨间（可配）：直接显示昨夜写的锚点内容。
  3. 22:00 若未记录交付物：「还有时间做一个 10 分钟版本。」
- iOS 16.4+ 且**添加到主屏幕**后才支持 Web Push → 首次使用引导安装 + 建议配一个系统闹钟兜底。
- 通知不可交互回复（PWA 限制），所以 App 内流程必须做到秒开秒填。

## 9. 技术方案

- **栈**：Vite + React + TypeScript + Tailwind CSS。
- **存储**：IndexedDB（Dexie），导出/导入 JSON + Markdown 周报。
- **PWA**：vite-plugin-pwa（manifest + Service Worker 离线可用 + standalone 全屏）。
- **关键 API**：Web Push、Screen Wake Lock、`display-mode: standalone` 检测引导安装。
- **部署**：任意静态托管（自用，无后端）。
- 计时器实现铁律：基于 `Date.now()` 时间戳差值，与渲染帧率、后台节流无关。

## 10. 实现顺序（范围是全六模式，但仍有先后）

1. **M1 心跳**：状态机 + 晨间确认 MIT + 深推计时器（含交付承诺）+ 记录交付物 → 最小可日用的闭环。
2. **M2 接力**：晚间复合流程 + 锚点跨天机制。
3. **M3 保护**：复位卡 + 应急模式 + 链条计算。
4. **M4 回望**：成果墙热力图 + 规则库 + 导入导出。
5. **M5 打磨**：PWA 安装引导 + 推送 + Wake Lock + 文案全量过一遍。

## 11. 反功能清单（明确不做）

- ❌ 社交动态、好友排行（对比是失控感的来源）
- ❌ 无限统计图表、复杂项目管理（Notion 化 = 死亡）
- ❌ 主题/字体/外观折腾（工具成瘾温床）
- ❌ 一天多个 MIT（系统明确：只许一个）
- ❌ 白噪音、专注森林等装饰性功能
- ❌ 番茄钟数据焦虑（专注轮数只是记录，不设 KPI）

## 12. 待定问题

- [ ] App 名字（当前工作名：重启24）
- [x] 视觉基调：暖色纸质（米白底 #f8f2e7 + 墨色 + 赭橙强调 #c05f1c）
- [x] 文案语气：教练式（简短、直接、有担当地给指令）
- [x] 部署：构建产物 dist/ 纯静态，任意静态托管可用；通知采用「应用内横幅 + 页面通知尽力而为 + 系统闹钟兜底」策略
